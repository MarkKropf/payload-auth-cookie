import type { CollectionConfig } from 'payload';
/**
 * Creates a users collection with SSO cookie authentication fields
 */
export declare function createUsersCollection(slug: string, isAdmin?: boolean): CollectionConfig;
